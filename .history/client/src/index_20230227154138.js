import React from 'react';
import ReactDOM from 'react-dom';
import { Provider } from 'react-redux'
imort { createStore, applyMiddleware, }
import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));